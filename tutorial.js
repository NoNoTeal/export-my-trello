const { INPUT,OUTPUT } = require('./editme.json');
console.log(`New to this? Here's a few starters.
Don't use node .
Use npm run runAll to rip board IDs provided in ./editme.json and then convert to CSV format
Use npm run getJSON to get JSON files from ./editme.json
Use npm run getCSV to get JSON files from ./${INPUT} and convert it to CSV to ./${OUTPUT}`);